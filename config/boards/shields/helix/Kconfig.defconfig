if SHIELD_FLUX_LEFT

config ZMK_KEYBOARD_NAME
    default "Flux KB"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_FLUX_LEFT || SHIELD_FLUX_RIGHT

config ZMK_SPLIT
    default y

endif